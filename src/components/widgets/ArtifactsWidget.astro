---
import WidgetWrapper from '~/components/ui/WidgetWrapper.astro';
import Headline from '~/components/ui/Headline.astro';

const artifacts = [
  {
    title: 'Design Document',
    description: 'System architecture, component design, and technical specifications for Tiger Rescue.',
    driveId: 'PLACEHOLDER_ID_1',
  },
  {
    title: 'Requirements Document',
    description: 'Functional and non-functional requirements gathered from sponsor and stakeholder meetings.',
    driveId: 'PLACEHOLDER_ID_2',
  },
  {
    title: 'Sprint 1 Report',
    description: 'Summary of Sprint 1 progress, deliverables completed, and retrospective notes.',
    driveId: 'PLACEHOLDER_ID_3',
  },
];
---

<WidgetWrapper containerClass="max-w-4xl mx-auto">
  <Headline
    title="Project Artifacts"
    subtitle="All project documentation in one place. Click any document to expand."
    tagline="Tiger Rescue"
  />

  <div class="flex flex-col gap-px">
    {artifacts.map((doc, i) => (
      <div class="accordion-item border border-gray-200 dark:border-gray-700 rounded-lg overflow-hidden mb-2 transition-colors duration-200">
        <button
          class="accordion-trigger w-full flex items-center justify-between px-6 py-4 bg-white dark:bg-slate-900 hover:bg-gray-50 dark:hover:bg-slate-800 transition-colors duration-150 text-left gap-4"
          aria-expanded="false"
          aria-controls={`panel-${i}`}
        >
          <div class="flex items-center gap-4 flex-1 min-w-0">
            <span class="text-xs font-medium text-primary tabular-nums flex-shrink-0">
              {String(i + 1).padStart(2, '0')}
            </span>
            <div class="flex flex-col gap-0.5 min-w-0">
              <span class="text-base font-semibold text-gray-900 dark:text-white">{doc.title}</span>
              <span class="text-sm text-muted truncate hidden sm:block">{doc.description}</span>
            </div>
          </div>

          <div class="flex items-center gap-3 flex-shrink-0">
            <a
              href={`https://drive.google.com/file/d/${doc.driveId}/view`}
              target="_blank"
              rel="noopener noreferrer"
              class="hidden sm:inline-flex items-center text-xs font-medium text-primary border border-primary rounded px-3 py-1.5 hover:bg-primary hover:text-white transition-colors duration-150 whitespace-nowrap"
              onclick="event.stopPropagation()"
            >
              Open ↗
            </a>
            <span class="chevron text-muted transition-transform duration-300">
              <svg width="18" height="18" viewBox="0 0 20 20" fill="none">
                <path d="M5 7.5L10 12.5L15 7.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </span>
          </div>
        </button>

        <div class="accordion-panel grid" style="grid-template-rows: 0fr;" id={`panel-${i}`} role="region">
          <div class="overflow-hidden">
            <iframe
              src={`https://drive.google.com/file/d/${doc.driveId}/preview`}
              class="block w-full h-[800px] sm:h-[800px] border-t border-gray-200 dark:border-gray-700"
              allow="autoplay"
              loading="lazy"
              title={doc.title}
            ></iframe>
          </div>
        </div>
      </div>
    ))}
  </div>
</WidgetWrapper>

<style>
  .accordion-item.is-open {
    @apply border-primary;
  }

  .accordion-item.is-open .chevron {
    transform: rotate(180deg);
    @apply text-primary;
  }

  .accordion-panel {
    transition: grid-template-rows 0.35s ease;
  }

  .accordion-panel.is-open {
    grid-template-rows: 1fr !important;
  }

  @media (max-width: 640px) {
    iframe {
      height: 500px;
    }
  }
</style>

<script>
  const items = document.querySelectorAll('.accordion-item');

  items.forEach((item) => {
    const trigger = item.querySelector('.accordion-trigger');
    const panel = item.querySelector('.accordion-panel');

    trigger?.addEventListener('click', () => {
      const isOpen = item.classList.contains('is-open');

      // Close all
      items.forEach((el) => {
        el.classList.remove('is-open');
        el.querySelector('.accordion-panel')?.classList.remove('is-open');
        el.querySelector('.accordion-trigger')?.setAttribute('aria-expanded', 'false');
      });

      // Open clicked if it was closed
      if (!isOpen) {
        item.classList.add('is-open');
        panel?.classList.add('is-open');
        trigger.setAttribute('aria-expanded', 'true');
      }
    });
  });
</script>